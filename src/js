var sum = function(){
    var sum = 0;
    for (i = 0;i < 100;i++){
        sum += i;
    }
        return sum;
}
var test = function(){
    return "hello, world";
}
var value = sum(); // value 是1+2+3+...+100的结果

var name = person.name;
var age  = person.age;

